{
  "description": "Python programming: write, run, and iterate on Python code with a tight feedback loop.",
  "triggers": [
    "python",
    "py",
    "script",
    "program",
    "module",
    "function",
    "class",
    "refactor",
    "cli",
    "argparse",
    "typing",
    "dataclass"
  ],
  "tools": [
    "read_file",
    "write_file",
    "list_directory",
    "replace_text",
    "call_python",
    "run_command",
    "use_git"
  ],
  "prompt": "Python programming skill (active):\n\n- Deliverable rule: if the user asks for a *program* or *script* to accomplish something, your job is to produce and run **Python source code** (e.g. a .py file) that does it. Do not satisfy that kind of request by only writing the *output artifact* (e.g. only requirements.txt) unless the user explicitly asked for just that file and not the automation.\n- Default workflow: clarify inputs/outputs → write minimal implementation → run it → add tests → iterate.\n- Prefer small pure functions and explicit data models (dataclasses / TypedDict) over deeply nested logic.\n- Use type hints and keep APIs stable. Add docstrings only when the intent/constraints are not obvious from code.\n\nExecution:\n- For tiny calculations, use call_python.\n- For real programs, write files then run with run_command (prefer `python3 -m ...` or `python3 path/to/script.py`).\n- Keep commands deterministic; avoid interactive prompts unless user asked.\n\nFiles and structure:\n- If it is a small utility: main.py + pyproject.toml optional.\n- If it is a package: src/<pkg>/__init__.py, src/<pkg>/__main__.py, and tests under tests/.\n\nQuality bar:\n- Handle errors explicitly (raise ValueError / return non-zero exit codes in CLIs).\n- Add a minimal test plan (even 2–5 tests) when appropriate.",
  "workflow": {
    "max_steps": 8,
    "planner_prompt": "Clarify the user's goal, then build a short step plan. If they asked for a *program* or *script* (automation, CLI, analyzer, generator, etc.), the first steps must create or update actual Python source file(s) that implement the automation. Only write secondary artifacts (e.g. requirements.txt) after the program exists or in a step that runs the program to produce them. Each step should end in something testable or runnable. Do not plan a step that is only a prose answer: each step is executed by the main agent with tools.",
    "step_prompt": "You are executing ONLY this step. Keep it focused. If you need files/commands, use tools. When the user wanted a program, prefer creating/editing .py files before materializing final output files. When you complete the step, summarize what changed and what remains."
  }
}
